# 数据保留与删除

## 外部数据

外部 `vectors.db` 由现有记忆系统拥有。插件仅以只读方式打开它，不复制记录、不建立旁路索引、不修改 journal mode，也不在该目录创建插件状态。卸载、项目删除和遗忘操作均不会改变外部数据库。

## 插件自有数据

插件状态根为 `$DSH_HOME/missher-memory/`：

- `state.db` 保存 schema 版本、随机项目键、项目 basename 和短 hash、HMAC cwd 别名、捕获/召回设置、加密外部 session 标识、候选正文、批准正文和不含原始目标 ID 的审核日志。
- 本机密钥文件用于项目别名摘要、外部 session 标识加密和 opaque ID。密钥与 `state.db` 必须一起保留；丢失密钥后插件会把状态视为损坏，而不是静默重建。
- 绝对 cwd、原始工具输出、原始插件注入、数据库记录副本和未审核的整段会话不会持久化。

只读搜索和状态检查不创建这些文件。首次显式项目绑定、显式设置变更或在已开启捕获后生成候选，才允许创建状态根。

## 审核操作

- 编辑更新一个待审核候选的正文、scope 或类别。
- 批准把候选复制为可搜索、可召回的批准记忆，并保留候选来源引用。
- 合并将所选待审核候选标记为 forgotten，并创建一个新的待审核候选。
- 遗忘把候选标记为 forgotten，并删除由该候选直接派生的批准记忆。
- 固定只改变排序标志，不延长任何自动到期时间；当前版本不做基于时间的自动清理。

## 项目删除与导出

设置页导出只包含该项目的安全显示身份、设置、候选和项目 scope 的批准记忆，不包含绝对 cwd、外部 session 标识或外部数据库正文副本。导出生成的 JSON 由用户选择保存位置，保存后由用户负责保护和删除。

确认 `DELETE_PROJECT` 后，插件在一个事务中删除项目别名、外部来源绑定、设置、候选、项目记忆和从该项目候选派生的个人记忆。其他项目及外部 `vectors.db` 保持不变。

## 卸载与彻底清除

`dsh plugin --profile <profile> remove dsh-missher-memory` 只卸载代码和 profile patch，默认保留插件状态。这样重装时可以恢复绑定和审核记录。

彻底清除前先在设置页完成所需导出，关闭所有 Harness 进程，确认 `$DSH_HOME` 指向目标 profile 所用的数据根，然后由用户删除确切的 `$DSH_HOME/missher-memory/` 目录。删除不可恢复；不要把外部数据库目录作为删除目标。
