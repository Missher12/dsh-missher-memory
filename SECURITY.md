# 安全模型

## 信任范围

DeepSeek Harness Host、用户显式确认的项目绑定和插件自有状态属于可信控制面。外部 `vectors.db` 中的正文、时间和历史指令都视为不可信数据，只能作为带来源的搜索结果或预算内召回材料，不能授予权限或改变工具安全策略。

## 外部数据库

插件只接受现有绝对目录中的普通 `vectors.db`。目录和文件必须不是符号链接，数据库规范路径必须仍位于规范根目录中。所有读取使用 Node 内建 SQLite、`readOnly: true`、禁止扩展加载和固定预编译 SQL；FTS 查询会转换为字面量 token。同步 SQLite 放在 Worker 中，超时会终止 Worker。

插件不执行 Python、shell 或数据库目录中的脚本，不复制、迁移、修复或初始化外部数据库。缺失数据库明确显示“未配置”。

## 项目身份与隔离

cwd 仅保存在 Host 内存中，供用户确认绑定。状态库保存 HMAC 项目别名、随机项目键、basename 和短 hash，不保存绝对 cwd。外部 session 标识以本机随机密钥经 AES-GCM 加密，并另存不可逆摘要用于唯一性判断。RPC 和设置页只接收短期 opaque handle，不暴露 cwd 或 session 标识。

每次搜索和召回都先按当前 cwd 查找权威绑定，再只使用该项目绑定的 session 标识。个人偏好存储为独立 scope；未经绑定的会话不能读取项目或个人记忆。

## 写入与隐私

只读搜索、状态查看、未绑定 session 事件和默认关闭的捕获都不会创建插件状态。`state.db` 和本机密钥只在用户显式绑定项目、修改设置或已启用捕获后的候选写入时创建，目录权限限制为当前用户。

候选正文、批准正文和导出内容是用户数据，以明文存于插件自有 SQLite。插件拒绝记录命名凭据、常见 token、私钥、数据库连接串、身份证号、金融号码和敏感用户路径；检测是保守的模式匹配，不替代用户审核或磁盘加密。工具结果、插件消息、子代理会话和自动召回内容不会进入候选捕获。

## 失败模式

初始化异常会记录不含内部路径或凭据值的稳定诊断并放弃注册。搜索缺失、路径不安全、损坏、不兼容和超时均返回非敏感状态；召回异常不注入；候选异常不阻止 session disposal。插件状态 schema 高于支持版本时拒绝写入，不做降级迁移。

## 操作建议

- 在绑定前人工确认来源计数和时间范围，避免把其他项目历史归入当前项目。
- 只在需要时开启捕获或召回，并定期审核、遗忘或删除项目记忆。
- 保护 `$DSH_HOME/missher-memory/` 的文件权限和设备磁盘加密；导出文件由浏览器下载后不再受插件控制。
- 如果源记忆系统或其脚本中出现过明文凭据，应在源系统侧轮换凭据并改为安全注入；本插件不会读取这些脚本。
